ext {
    gradle_version = '3.3.2'
    kotlin_version = '1.3.21'

    retrofit = '2.5.0'
    okhttp = '3.14.0'
    moshi = '1.8.0'
    dagger = '2.21'

    networkingDependencies = [
            retrofit2 : "com.squareup.retrofit2:retrofit:${retrofit}",
            okhttp3 : "com.squareup.okhttp3:okhttp:${okhttp}",
            jsonParsing : [
                    moshi : "com.squareup.moshi:moshi:${moshi}"
            ]
    ]

    dependencyInjection = [
            dagger2 : [
                    annotation : "com.google.dagger:dagger:${dagger}",
                    processor : "com.google.dagger:dagger-compiler:${dagger}"
            ]
    ]
}